name: CD

on:
  push:
    # Sequence of patterns matched against refs/heads
    branches:
      - 'devel'
      - 'main'
      - 'master'
      - 'ballercast'
    tags:
      - '*' # Test all pushed tags

  pull_request:
    types:
      - labeled
      - unlabeled
      - synchronize
      - opened
      - edited
      - ready_for_review
      - reopened
      - unlocked

  pull_request_review:
    types:
      - submitted
env:
  RELEASE: release
  ID_RSA_PRIV: ${{ secrets.ID_RSA_PRIV }}
  ID_RSA_PUB: ${{ secrets.ID_RSA_PUB }}

jobs:
  build:
    runs-on: ubuntu-latest
    name: Simply build for deployment
    steps:

    - uses: actions/checkout@v3
      with:
        submodules: 'recursive'

    - name: Setup Node.js 20
      uses: actions/setup-node@v3
      with:
        node-version: "20"

    - name: Extract secrets
      if: github.ref == 'refs/heads/ballercast' || (github.event_name == 'pull_request' && github.base_ref == 'ballercast')
      run: |
        cd secrets
        ./extract_secrets.sh ${{ secrets.CREDENTIALS_AND_ID_PUB }}

    - name: install and lint
      run: |
        npm ci
        npm run lint

    - name: build
      run: |
        npm run build

    - name: Upload
      if: github.event_name == 'push' && (github.ref == 'refs/heads/ballercast' || github.ref == 'refs/heads/master' || github.ref == 'refs/heads/main')
      run: |
        echo "$ID_RSA_PRIV" > id_rsa_sftp
        echo "$ID_RSA_PUB" > id_rsa_sftp.pub
        chmod 600 id_rsa_sftp
        chmod 644 id_rsa_sftp.pub
        ./scripts/sftp_put.sh
        rm id_rsa_sftp
        rm id_rsa_sftp.pub