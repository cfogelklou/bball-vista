/**
 * @ballercast/shared-core
 *
 * Shared TypeScript library for BallerCast applications
 * Contains cross-platform game state management, Firebase utilities, and reusable components
 */

// Types

// Re-export common types for convenience
export type {
  TeamState,
  ClockState,
  GameState
} from '@common/types/gameState';